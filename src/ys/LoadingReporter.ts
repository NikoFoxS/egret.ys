module ys {
	export class LoadingReporter implements RES.PromiseTaskReporter {
		public constructor() {
		}

		public onReady(): void {
		}

		public onStart(groupName: string): void {
		}

		public onProgress(current: number, total: number, resItem: RES.ResourceInfo) {
		}

		public onLoaded(groupName): void {
		}

	}
}